apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: org
      defaultBucket: SmartThings
    secureJsonData:
      token: token
    isDefault: true
    editable: true